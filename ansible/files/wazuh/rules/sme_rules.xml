<group name="sme_security,">
  <rule id="100101" level="6">
    <if_sid>2501</if_sid>
    <field name="app">^core$</field>
    <field name="message" type="pcre2">^Login failed:</field>
    <description>Nextcloud login failure from $(remoteAddr) for $(user).</description>
    <mitre><id>T1110</id></mitre>
    <group>nextcloud,authentication_failed,</group>
  </rule>

  <rule id="100102" level="10" frequency="5" timeframe="120">
    <if_matched_sid>100101</if_matched_sid>
    <same_field>remoteAddr</same_field>
    <description>Multiple Nextcloud login failures from the same source.</description>
    <mitre><id>T1110</id></mitre>
    <group>nextcloud,authentication_failures,brute_force,</group>
  </rule>

  <rule id="100200" level="0">
    <decoded_as>sme-keycloak-event</decoded_as>
    <description>Keycloak event.</description>
    <group>keycloak,</group>
  </rule>

  <rule id="100201" level="6">
    <if_sid>100200</if_sid>
    <field name="event_type">^LOGIN_ERROR$</field>
    <description>Keycloak login failure from $(srcip): $(error).</description>
    <mitre><id>T1110</id></mitre>
    <group>keycloak,authentication_failed,</group>
  </rule>

  <rule id="100202" level="10" frequency="5" timeframe="120">
    <if_matched_sid>100201</if_matched_sid>
    <same_source_ip />
    <description>Multiple Keycloak login failures from the same source.</description>
    <mitre><id>T1110</id></mitre>
    <group>keycloak,authentication_failures,brute_force,</group>
  </rule>

  <rule id="100301" level="10" frequency="5" timeframe="120">
    <if_matched_sid>9705</if_matched_sid>
    <same_source_ip />
    <description>Multiple Dovecot login failures from the same source.</description>
    <mitre><id>T1110</id></mitre>
    <group>mail,dovecot,authentication_failures,brute_force,</group>
  </rule>

  <rule id="100401" level="8">
    <if_sid>1002</if_sid>
    <match type="pcre2">Failed to bind to uuid.*NT_STATUS_LOGON_FAILURE</match>
    <description>Samba AD replication bind failed authentication.</description>
    <group>samba,active_directory,replication,authentication_failed,</group>
  </rule>

  <rule id="100500" level="0">
    <decoded_as>json</decoded_as>
    <field name="win.system.providerName">^Microsoft-Windows-</field>
    <field name="win.system.channel">\.+</field>
    <description>Windows JSON fixture or integration event.</description>
    <group>windows,</group>
  </rule>

  <rule id="100501" level="7">
    <if_sid>60001,100500</if_sid>
    <field name="win.system.providerName">^Microsoft-Windows-Security-Auditing$</field>
    <field name="win.system.channel">^Security$</field>
    <field name="win.system.eventID">^4625$</field>
    <description>Windows failed logon on $(win.system.computer) for $(win.eventdata.targetUserName) from $(win.eventdata.ipAddress).</description>
    <mitre><id>T1110</id></mitre>
    <group>windows,authentication_failed,</group>
  </rule>

  <rule id="100502" level="10" frequency="5" timeframe="120">
    <if_matched_sid>100501</if_matched_sid>
    <same_field>win.eventdata.ipAddress</same_field>
    <description>Multiple Windows failed logons from the same source.</description>
    <mitre><id>T1110</id></mitre>
    <group>windows,authentication_failures,brute_force,</group>
  </rule>

  <rule id="100503" level="3">
    <if_sid>60001,100500</if_sid>
    <field name="win.system.providerName">^Microsoft-Windows-Security-Auditing$</field>
    <field name="win.system.channel">^Security$</field>
    <field name="win.system.eventID">^4624$</field>
    <field name="win.eventdata.logonType">^2$|^10$</field>
    <description>Windows interactive logon on $(win.system.computer) for $(win.eventdata.targetUserName).</description>
    <group>windows,authentication_success,</group>
  </rule>

  <rule id="100504" level="5">
    <if_sid>100500</if_sid>
    <field name="win.system.providerName">^Microsoft-Windows-PowerShell$</field>
    <field name="win.system.eventID">^4104$</field>
    <field name="win.system.channel">^Microsoft-Windows-PowerShell/Operational$</field>
    <field name="win.eventdata.scriptBlockText" type="pcre2">WAZUH_4104_VALIDATION_[A-Z0-9_-]+</field>
    <description>Windows PowerShell 4104 JSON integration validation marker observed on $(win.system.computer).</description>
    <mitre><id>T1059.001</id></mitre>
    <group>windows,powershell,integration_validation,</group>
  </rule>

  <rule id="100505" level="12">
    <if_sid>62123,100500</if_sid>
    <field name="win.system.providerName">^Microsoft-Windows-Windows Defender$</field>
    <field name="win.system.channel">^Microsoft-Windows-Windows Defender/Operational$</field>
    <field name="win.system.eventID">^1116$</field>
    <description>Microsoft Defender detected malware on $(win.system.computer): $(win.eventdata.threatName).</description>
    <mitre><id>T1204</id></mitre>
    <group>windows,defender,malware,</group>
  </rule>

</group>
