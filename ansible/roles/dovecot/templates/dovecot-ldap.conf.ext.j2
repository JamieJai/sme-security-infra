hosts = 192.168.0.20:389
ldap_version = 3
tls = no
tls_require_cert = never

base = dc=toss,dc=lan
scope = subtree

auth_bind = yes
auth_bind_userdn = %u@toss.lan

user_filter = (&(objectClass=user)(sAMAccountName=%u))
user_attrs = =home=/home/%u,=mail=maildir:~/Maildir
